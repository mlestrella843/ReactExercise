

// estos son los operadores condicional terciarios, funciona como un if
//si es aquello sale una respuesta, sino la otra.

const activo = true;

// const mensaje = ( activo ) ? 'Activo' : 'Inactivo';

const mensaje = activo && 'Activo';

console.log( mensaje );